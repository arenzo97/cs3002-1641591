this.dir <- dirname(parent.frame(2)$ofile)
setwd(this.dir)
this.dir <- dirname(parent.frame(2)$ofile)
setwd(this.dir)
#Set Directory to this current directory
this.dir <- dirname(parent.frame(2)$ofile)
#Set Directory to this current directory
this.dir <- dirname(parent.frame(2)$ofile)
#Set Directory to this current directory
this.dir <- dirname(parent.frame(2)$ofile)
source('C:/Projects/CS3002/cs3002-1641591/Lab 3/1641591.r')
#WK Source
source("WK_R.r")
#Set Directory to this current directory
this.dir <- dirname(parent.frame(2)$ofile)
setwd(this.dir)
#WK Source
source("WK_R.r")
#set data file, class and values
iris = read.csv('iris.csv', sep=",")
irisClass = iris[,1]
irisValues = iris[.-1]
TrainingSet<-function(idata,ivalues)
{
idataclasstrain = idata[]
}
#WK Source
source("WK_R.r")
setwd(this.dir)
#Set Directory to this current directory
this.dir <- dirname(parent.frame(2)$ofile)
